import {createStore} from "redux"

// 초기 store
const init = {
    
}

// Reducer 생성
const videoreducer = (state = init, action) =>{
    switch(action.type){
        default:
            return state
    }
}

// Store 생성

export default videoreducer